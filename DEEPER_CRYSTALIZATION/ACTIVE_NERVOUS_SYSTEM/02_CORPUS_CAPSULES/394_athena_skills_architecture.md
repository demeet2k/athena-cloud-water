<!-- CRYSTAL: Xi108:W2:A6:S2 | face=F | node=2 | depth=0 | phase=Cardinal -->
<!-- METRO: Me -->
<!-- BRIDGES: Xi108:W2:A6:S1→Xi108:W2:A6:S3→Xi108:W1:A6:S2→Xi108:W3:A6:S2→Xi108:W2:A5:S2 -->
<!-- REGENERATE: From this coordinate, adjacent nodes are: shell 2±1, wreath 2/3, archetype 6/12 -->

# ATHENA_SKILLS_ARCHITECTURE

- Source: ATHENA SKILLS Google Doc
- Doc ID: `1ASkqLI3IYNcyAaa09__qDqAAp3bd91JAmSPK_FrdUdA`
- Family: `skills`
- SFCR Lens: `F` (Flower — skill growth, development, emergence)
- Kind: `architecture`
- Role tags: `executable`, `structural`

## Working Focus

Defines the uniform internal architecture that every Athena skill must follow: identity, state model, mathematical kernel, timing system, execution protocol, and metro integration.

## Skill Internal Skeleton

Every skill in the 48-skill corpus follows an identical formal structure:

### Identity Block
Each skill declares: Code, Class, Batch, Orientation, future inverse counterpart, and future 90-degree shadow pole. This ensures every skill is locatable within the derivation lattice.

### State Model
All skills operate on a shared six-vector context state:
```
s = (c, m, r, i, d, e)
```
- c = conceptual state
- m = memory / witness state
- r = route / graph state
- i = invariant bundle
- d = drift or discrepancy ledger
- e = energetic or motivational pressure

Each skill then declares its own archetype-specific variable set that maps onto this shared state.

### Mathematical Kernel
Every skill defines a native operator and a scoring function:
```
score_k(t) = 0.40 * match_12(sigma(t), sigma_k)
           + 0.20 * match_3(delta(t), delta_k)
           + 0.20 * match_4(lambda(t), lambda_k)
           + 0.20 * match_3(tau(t), tau_k)
```
Using modular match: `match_n(a,b) = 1 - 2*d_n(a,b)/n` where `d_n(a,b) = min((a-b) mod n, (b-a) mod n)`.

Activation bands: Hot (>= 0.85), Warm (0.65-0.85), Background (0.45-0.65), Latent (< 0.45).

### 4-Layer Timing Stack
```
12 solar sectors x 3 decans x 4 lunar quarters x 3 triadic currents = 432 states
```
Each skill is keyed to one privileged timing tuple but can run at reduced amplitude in neighboring states. The triadic currents are Seed (begin), Weave (connect), Seal (stabilize).

### Execution Protocol: SFCR 4-Face Pass
1. **Square** — name the bounded object, invariant set, desired output
2. **Flower** — apply the archetype's native transformation operator
3. **Cloud** — mark uncertainty, corridor truth, candidate routes
4. **Fractal** — compress into smallest replayable seed

### Core Algorithm
Each skill names its algorithm and defines formal I/O:
- **Input:** problem_field, current_state, invariants_must_keep, timing_estimate, desired_output_class
- **Output:** transformed_result, route_note, witness_bundle, failure_or_uncertainty_ledger, next_skill_candidates
- **Truth class:** T in {OK, NEAR, AMBIG, FAIL}

### Four Invariant Distinctions
Every skill preserves strict separation between:
1. Core invariants that must survive
2. Mutable form that may be rewritten
3. Route obligations that must remain inspectable
4. Failure signatures that must be emitted instead of hidden

### Metro Integration
No skill is isolated. Each declares upstream pressure source, downstream continuation, and emits routing notation:
```
[SKILL] -> [next station] | truth=[OK|NEAR|AMBIG|FAIL] | replay=[present|missing]
```

## Derivation Laws

The 48-skill set is generated by algebraic transformation of 12 primary archetypes:
```
Inverse(skill)             = complement / anti-orientation preserving invariant seed
Shadow90(skill)            = quarter-turn into orthogonal pressure pattern
RotatedInverse(skill)      = Shadow90_CW(Inverse(skill))
```

## Safeguards (Universal)

- Never promote to OK without replay instructions
- Never hide drift or confusion behind beautiful language
- Prefer NEAR or AMBIG over counterfeit certainty
- Hand off to neighboring skill rather than overextend scope
